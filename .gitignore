# ============================================================
#  NEVER COMMIT SECRETS OR MACHINE-LOCAL STATE
# ============================================================

# 🔴 Live OAuth tokens / API keys — re-login on each machine instead
auth.json
**/auth.json

# Installed packages / clones / caches — regenerated by `pi install`
agent/npm/
agent/git/
agent/bin/
agent/fff/
agent/memory/
agent/web-search-cache/

# Machine-local history & rebuildable state
agent/sessions/
agent/run-history.jsonl
agent/models-store.json
agent/crashes.json

# Python / tooling cruft
**/__pycache__/
*.pyc

# OS noise
.DS_Store
Thumbs.db
